// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v3.20.3
// source: sbai_protos/register_payload.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { PayloadIdentity } from "./payload_identity";
import { RequestHeader } from "./request_header";

export const protobufPackage = "sbai_protos";

export interface RegisterPayloadRequest {
  /** Header for the request. */
  header:
    | RequestHeader
    | undefined;
  /** Identity of the payload to register. */
  payloadIdentity: PayloadIdentity | undefined;
}

export interface RegisterPayloadResponse {
  /** True if the payload was registered successfully. */
  success: boolean;
  /** Error message if the registration failed. */
  errorMessage: string;
}

function createBaseRegisterPayloadRequest(): RegisterPayloadRequest {
  return { header: undefined, payloadIdentity: undefined };
}

export const RegisterPayloadRequest: MessageFns<RegisterPayloadRequest> = {
  encode(message: RegisterPayloadRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.header !== undefined) {
      RequestHeader.encode(message.header, writer.uint32(10).fork()).join();
    }
    if (message.payloadIdentity !== undefined) {
      PayloadIdentity.encode(message.payloadIdentity, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RegisterPayloadRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRegisterPayloadRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.header = RequestHeader.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.payloadIdentity = PayloadIdentity.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RegisterPayloadRequest {
    return {
      header: isSet(object.header) ? RequestHeader.fromJSON(object.header) : undefined,
      payloadIdentity: isSet(object.payloadIdentity) ? PayloadIdentity.fromJSON(object.payloadIdentity) : undefined,
    };
  },

  toJSON(message: RegisterPayloadRequest): unknown {
    const obj: any = {};
    if (message.header !== undefined) {
      obj.header = RequestHeader.toJSON(message.header);
    }
    if (message.payloadIdentity !== undefined) {
      obj.payloadIdentity = PayloadIdentity.toJSON(message.payloadIdentity);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RegisterPayloadRequest>, I>>(base?: I): RegisterPayloadRequest {
    return RegisterPayloadRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RegisterPayloadRequest>, I>>(object: I): RegisterPayloadRequest {
    const message = createBaseRegisterPayloadRequest();
    message.header = (object.header !== undefined && object.header !== null)
      ? RequestHeader.fromPartial(object.header)
      : undefined;
    message.payloadIdentity = (object.payloadIdentity !== undefined && object.payloadIdentity !== null)
      ? PayloadIdentity.fromPartial(object.payloadIdentity)
      : undefined;
    return message;
  },
};

function createBaseRegisterPayloadResponse(): RegisterPayloadResponse {
  return { success: false, errorMessage: "" };
}

export const RegisterPayloadResponse: MessageFns<RegisterPayloadResponse> = {
  encode(message: RegisterPayloadResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.success !== false) {
      writer.uint32(8).bool(message.success);
    }
    if (message.errorMessage !== "") {
      writer.uint32(18).string(message.errorMessage);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RegisterPayloadResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRegisterPayloadResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.errorMessage = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RegisterPayloadResponse {
    return {
      success: isSet(object.success) ? globalThis.Boolean(object.success) : false,
      errorMessage: isSet(object.errorMessage) ? globalThis.String(object.errorMessage) : "",
    };
  },

  toJSON(message: RegisterPayloadResponse): unknown {
    const obj: any = {};
    if (message.success !== false) {
      obj.success = message.success;
    }
    if (message.errorMessage !== "") {
      obj.errorMessage = message.errorMessage;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RegisterPayloadResponse>, I>>(base?: I): RegisterPayloadResponse {
    return RegisterPayloadResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RegisterPayloadResponse>, I>>(object: I): RegisterPayloadResponse {
    const message = createBaseRegisterPayloadResponse();
    message.success = object.success ?? false;
    message.errorMessage = object.errorMessage ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
